{
  "name": "sparklab",
  "version": "1.0.0",
  "description": "A virtual electronics workbench for learning circuits without buying a kit.",
  "private": true,
  "scripts": {
    "start": "npx --yes serve . -l 8080",
    "test": "node tests/circuit.test.js && node tests/touch.test.js"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
